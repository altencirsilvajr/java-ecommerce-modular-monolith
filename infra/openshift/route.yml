apiVersion: route.openshift.io/v1
kind: Route
metadata: {name: ecommerce-api}
spec:
  to: {kind: Service, name: ecommerce-api}
  port: {targetPort: http}
  tls: {termination: edge, insecureEdgeTerminationPolicy: Redirect}
