server {
  listen 80;
  root /usr/share/nginx/html;
  location /api/ { proxy_pass http://api:8080; proxy_set_header X-Correlation-ID $request_id; }
  location /actuator/ { proxy_pass http://api:8080; }
  location / { try_files $uri $uri/ /index.html; }
}
